@model Portfolio.Models.Projects

@{
    // Use shared admin layout
    Layout = "~/Views/Shared/_AdminLayout.cshtml";

    // Page title shown in layout/topbar
    ViewBag.Title = "Proje Düzenle";

    // Determine current project image or fallback to default image
    var currentProjectImage = !string.IsNullOrEmpty(Model.ThumbnailUrl)
        ? Url.Content(Model.ThumbnailUrl)
        : Url.Content("~/Content/img/default-project.jpg");
}

<style>
    /* Main edit card container */
    .project-edit-card {
        border-radius: 24px;
    }

    /* Preview panel used for image upload area */
    .preview-panel {
        border-radius: 20px;
        background: #f8f9fa;
    }

    /* Project image preview styling */
    .project-preview {
        width: 100%;
        max-height: 320px;
        object-fit: cover;
        border-radius: 18px;
    }

    /* Small uppercase section title */
    .form-section-title {
        font-size: 0.95rem;
        font-weight: 700;
        color: #6c757d;
        text-transform: uppercase;
        letter-spacing: 0.04em;
        margin-bottom: 1rem;
    }

    /* File upload help text */
    .file-help {
        font-size: 0.85rem;
        color: #6c757d;
    }

    /* Client-side error box, hidden by default */
    .custom-error-box {
        display: none;
        border-radius: 16px;
    }

    /* Action bar at the bottom of the form */
    .action-bar {
        border-top: 1px solid #ececec;
        margin-top: 2rem;
        padding-top: 1.25rem;
    }

    /* Move preview column above content on smaller screens */
    @@media (max-width: 991.98px) {
        .preview-column {
            order: -1;
        }
    }
</style>

<!-- PAGE HEADER -->
<div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-2">
    <div>
        <h2 class="fw-bold mb-0">⚙️  Proje Düzenle</h2>
        <small class="text-muted">Proje bilgilerini ve görselini güncelle</small>
    </div>
</div>

<!-- Server-side error message -->
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger rounded-4 shadow-sm">
        @TempData["Error"]
    </div>
}

<!-- Client-side validation error box -->
<div id="clientErrorBox" class="alert alert-danger shadow-sm custom-error-box mb-4"></div>

<!-- MAIN EDIT CARD -->
<div class="card shadow-lg border-0 project-edit-card">
    <div class="card-body p-4 p-lg-5">
        <!-- Form for updating project information -->
        <form method="post"
              enctype="multipart/form-data"
              action="@Url.Action("Edit", "AdminProject")"
              id="projectEditForm"
              novalidate>
            @Html.AntiForgeryToken()

            <!-- Hidden identifiers -->
            <input type="hidden" name="ProjectId" value="@Model.ProjectId" />
            <input type="hidden" name="ExistingThumbnailUrl" value="@Model.ThumbnailUrl" />

            <div class="row g-4 align-items-start">
                <!-- LEFT COLUMN: PROJECT DATA -->
                <div class="col-lg-7">
                    <div class="form-section-title">Proje Bilgileri</div>

                    <!-- Project title -->
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Başlık</label>
                        <input type="text"
                               name="Title"
                               class="form-control form-control-lg"
                               value="@Model.Title"
                               required
                               maxlength="150" />
                    </div>

                    <!-- Short description -->
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Kısa Açıklama</label>
                        <input type="text"
                               name="ShortDescription"
                               class="form-control"
                               value="@Model.ShortDescription"
                               required
                               maxlength="300" />
                    </div>

                    <!-- Main description -->
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Ana Açıklama</label>
                        <textarea name="Description"
                                  class="form-control"
                                  rows="6"
                                  placeholder="Proje detay sayfasında görünecek ana açıklama">@Model.Description</textarea>
                    </div>

                    <!-- Technologies -->
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Teknolojiler</label>
                        <input type="text"
                               name="Technologies"
                               class="form-control"
                               value="@Model.Technologies"
                               placeholder="ASP.NET MVC, SQL Server, Bootstrap" />
                    </div>

                    <div class="row">
                        <!-- GitHub URL -->
                        <div class="col-md-6 mb-3">
                            <label class="form-label fw-semibold">
                                GitHub Linki <span class="text-muted">(opsiyonel)</span>
                            </label>
                            <input type="url"
                                   name="GithubUrl"
                                   class="form-control"
                                   value="@Model.GithubUrl"
                                   placeholder="https://github.com/..." />
                        </div>

                        <!-- Live demo URL -->
                        <div class="col-md-6 mb-3">
                            <label class="form-label fw-semibold">
                                Canlı Demo Linki <span class="text-muted">(opsiyonel)</span>
                            </label>
                            <input type="url"
                                   name="LiveDemoUrl"
                                   class="form-control"
                                   value="@Model.LiveDemoUrl"
                                   placeholder="https://..." />
                        </div>
                    </div>

                    <!-- Display order -->
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Sıra</label>
                        <input type="number"
                               name="DisplayOrder"
                               class="form-control"
                               value="@Model.DisplayOrder"
                               min="0" />
                    </div>

                    <!-- Project flags -->
                    <div class="d-flex flex-wrap gap-4 mb-3">
                        <div class="form-check">
                            <input type="checkbox"
                                   name="IsFeatured"
                                   value="true"
                                   class="form-check-input"
                                   id="isFeatured"
                                   @(Model.IsFeatured ? "checked" : "") />
                            <label class="form-check-label" for="isFeatured">Öne Çıkan</label>
                        </div>

                        <div class="form-check">
                            <input type="checkbox"
                                   name="IsActive"
                                   value="true"
                                   class="form-check-input"
                                   id="isActive"
                                   @(Model.IsActive ? "checked" : "") />
                            <label class="form-check-label" for="isActive">Aktif</label>
                        </div>
                    </div>
                </div>

                <!-- RIGHT COLUMN: IMAGE PREVIEW -->
                <div class="col-lg-5 preview-column">
                    <div class="preview-panel p-3 p-lg-4 shadow-sm border h-100">
                        <div class="form-section-title">Proje Görseli</div>

                        <!-- Image file input -->
                        <label class="form-label fw-semibold">Mevcut / Yeni Görsel</label>
                        <input type="file"
                               name="imageFile"
                               id="imageFile"
                               class="form-control mb-3"
                               accept=".jpg,.jpeg,.png,.webp,image/*" />

                        <!-- Upload rules -->
                        <div class="file-help mb-2">
                            JPG, JPEG, PNG veya WEBP. Maksimum 5MB.
                        </div>

                        <!-- Inline image validation message -->
                        <div id="imageValidationMessage" class="text-danger small mb-3"></div>

                        <!-- Current/new image preview -->
                        <img id="imagePreview"
                             src="@currentProjectImage"
                             class="img-fluid shadow-sm border project-preview"
                             alt="Proje Önizleme" />

                        <!-- File status text -->
                        <div class="mt-3 text-muted small" id="imageFileName">
                            @(string.IsNullOrEmpty(Model.ThumbnailUrl) ? "Henüz proje görseli yok" : "Mevcut proje görseli kullanılıyor")
                        </div>
                    </div>
                </div>
            </div>

            <!-- FORM ACTIONS -->
            <div class="action-bar d-flex flex-wrap gap-2 justify-content-between align-items-center">
                <div class="d-flex flex-wrap gap-2">
                    <button type="submit" class="btn btn-primary px-4" id="saveBtn">
                        Güncelle
                    </button>
                    <a href="@Url.Action("Index", "AdminProject")" class="btn btn-secondary">
                        Geri Dön
                    </a>
                </div>
            </div>
        </form>
    </div>
</div>

<script>
    // DOM references for image validation and preview
    const imageInput = document.getElementById("imageFile");
    const imagePreview = document.getElementById("imagePreview");
    const imageFileName = document.getElementById("imageFileName");
    const imageValidationMessage = document.getElementById("imageValidationMessage");

    const form = document.getElementById("projectEditForm");
    const saveBtn = document.getElementById("saveBtn");
    const clientErrorBox = document.getElementById("clientErrorBox");

    // Shows client-side validation error
    function showClientError(message) {
        clientErrorBox.style.display = "block";
        clientErrorBox.innerText = message;
        window.scrollTo({ top: 0, behavior: "smooth" });
    }

    // Clears client-side validation error
    function clearClientError() {
        clientErrorBox.style.display = "none";
        clientErrorBox.innerText = "";
    }

    // Validates project image file
    function validateImageFile(file) {
        if (!file) return { valid: true };

        const allowedTypes = [
            "image/jpeg",
            "image/png",
            "image/webp"
        ];

        const allowedExtensions = [".jpg", ".jpeg", ".png", ".webp"];
        const lowerName = file.name.toLowerCase();
        const hasValidExtension = allowedExtensions.some(ext => lowerName.endsWith(ext));

        if (!allowedTypes.includes(file.type) && !hasValidExtension) {
            return {
                valid: false,
                message: "Proje görseli için yalnızca JPG, JPEG, PNG veya WEBP yükleyebilirsin."
            };
        }

        if (file.size > 5 * 1024 * 1024) {
            return {
                valid: false,
                message: "Proje görseli maksimum 5MB olabilir."
            };
        }

        return { valid: true };
    }

    // Live validation and preview when a new image is selected
    if (imageInput) {
        imageInput.addEventListener("change", function () {
            imageValidationMessage.innerText = "";
            clearClientError();

            const file = this.files[0];
            const validation = validateImageFile(file);

            if (!validation.valid) {
                this.value = "";
                imageValidationMessage.innerText = validation.message;
                imageFileName.innerText = "Geçersiz dosya seçildi";
                showClientError(validation.message);
                return;
            }

            if (file) {
                imageFileName.innerText = file.name;

                const reader = new FileReader();
                reader.onload = function (e) {
                    imagePreview.src = e.target.result;
                };
                reader.readAsDataURL(file);
            } else {
                imageFileName.innerText = "Yeni görsel seçilmedi";
            }
        });
    }

    // Final validation before form submission
    form.addEventListener("submit", function (e) {
        clearClientError();
        imageValidationMessage.innerText = "";

        const imageFile = imageInput.files[0];

        if (imageFile) {
            const validation = validateImageFile(imageFile);

            if (!validation.valid) {
                e.preventDefault();
                imageValidationMessage.innerText = validation.message;
                showClientError(validation.message);
                return;
            }
        }

        // Prevent multiple submissions and show progress state
        saveBtn.disabled = true;
        saveBtn.innerText = "Güncelleniyor...";
    });
</script>